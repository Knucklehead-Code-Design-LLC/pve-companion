name: pve_companion
description: "A secure, Apple-first companion for Proxmox VE."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.0
  flutter: '>=3.44.0'

dependencies:
  crypto: ^3.0.7
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/brand/pve_companion_mark.png
