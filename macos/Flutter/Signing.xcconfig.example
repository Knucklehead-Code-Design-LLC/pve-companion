// Copy this file to Signing.xcconfig on a publishing Mac.
// Signing.xcconfig is intentionally ignored and must never contain a
// certificate, profile name, credential, or server configuration.
DEVELOPMENT_TEAM = YOUR_TEAM_ID
